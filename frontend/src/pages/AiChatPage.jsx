import ComingSoon from '../components/ComingSoon';

export default function AiChatPage() {
  return (
    <ComingSoon
      title="AI Chat"
      description="Conversational coaching powered by LangGraph agents (via Socket.IO + REST)."
    />
  );
}
